#include <stdio.h>
int main() {
    freopen("input.txt", "r", stdin);
freopen("output.txt", "w", stdout);
   // printf() displays the string inside quotation
   printf("Hello, World!");
   return 0;
}